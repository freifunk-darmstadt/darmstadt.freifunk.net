image: ruby:2.1

variables:
  LANG: "C.UTF-8"
  NOKOGIRI_USE_SYSTEM_LIBRARIES: "1"

stages:
  - build
  - test

build:
  stage: build
  script:
    - bundler
    - gem install rake
  artifacts:
    paths:
    - test

test:
  stage: test
  script:
    - bundle exec rake test
